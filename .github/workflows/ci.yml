name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"
  DASHBOARD_FRANKENTUI_REVISION: 8aeb72216c3a5e8b03029c9634e607adcc297402

jobs:
  # The browser dashboard crate is an independent nested workspace, so the
  # repository-wide workspace jobs below do not discover or validate it.
  dashboard-wasm:
    name: Dashboard WASM + Exporter Matrix
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Clone FrankenTUI sibling dependency
        run: |
          git init ../frankentui
          git -C ../frankentui remote add origin https://github.com/Dicklesworthstone/frankentui
          git -C ../frankentui fetch --depth 1 origin "$DASHBOARD_FRANKENTUI_REVISION"
          git -C ../frankentui checkout --detach FETCH_HEAD
          test "$(git -C ../frankentui rev-parse HEAD)" = "$DASHBOARD_FRANKENTUI_REVISION"

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
          targets: wasm32-unknown-unknown

      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
          workspaces: crates/mcp-agent-mail-dashboard-wasm

      - name: Check dashboard formatting
        run: cargo fmt --manifest-path crates/mcp-agent-mail-dashboard-wasm/Cargo.toml --all -- --check

      - name: Test default browser dashboard
        run: cargo test --manifest-path crates/mcp-agent-mail-dashboard-wasm/Cargo.toml --locked --all-targets

      - name: Test native exporter feature boundary
        run: >-
          cargo test
          --manifest-path crates/mcp-agent-mail-dashboard-wasm/Cargo.toml
          --locked
          --no-default-features
          --features exporter
          --all-targets

      - name: Lint native exporter feature boundary
        run: >-
          cargo clippy
          --manifest-path crates/mcp-agent-mail-dashboard-wasm/Cargo.toml
          --locked
          --no-default-features
          --features exporter
          --all-targets
          -- -D warnings

      - name: Build browser WASM release
        run: >-
          cargo build
          --manifest-path crates/mcp-agent-mail-dashboard-wasm/Cargo.toml
          --locked
          --target wasm32-unknown-unknown
          --release
          --lib

  # ── Build + Unit + Integration ──────────────────────────────────────
  test:
    name: Build & Test
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Clone sibling dependencies
        shell: bash
        run: |
          git clone --depth 1 https://github.com/Dicklesworthstone/frankensearch ../frankensearch
          git -C ../frankensearch fetch --depth 1 origin f4f3faf7d610c355c28e83939ceea3c0d8481cc5
          git -C ../frankensearch checkout --detach FETCH_HEAD
          # frankensearch's tools/optimize_params workspace member depends on
          # ../../../fast_cmaes — required for cargo metadata/fmt/clippy on the
          # full workspace (cargo build -p X can skip it, but workspace-wide
          # commands cannot).
          git clone --depth 1 https://github.com/Dicklesworthstone/fast_cmaes ../fast_cmaes
          # 2026-08-22 registry universe: asupersync, fastmcp, sqlmodel, fsqlite,
          # ftui, franken-agent-detection, tru, and rich_rust all resolve from
          # crates.io. beads_rust stays path-patched (registry 0.3.2 pins
          # asupersync =0.4.4; the 0.4.0 line on main pins =0.4.9).
          git clone --depth 1 https://github.com/Dicklesworthstone/beads_rust ../beads_rust

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true

      - name: Guard docs drift on live surfaces
        shell: bash
        run: |
          set -euo pipefail

          live_docs=(
            README.md
            AGENTS.md
            docs/VISION.md
            docs/OPERATOR_RUNBOOK.md
            docs/OPERATOR_COOKBOOK.md
            docs/RELEASE_CHECKLIST.md
            docs/SPEC-interface-mode-switch.md
            docs/SPEC-meta-command-allowlist.md
          )

          stale_pattern='(34 tools|36 tools|20\+ resources|33 resources|15 screens|11 screens|mcp-agent-mail serve-http|mcp-agent-mail serve-stdio|mcp-agent-mail-cli share|mcp-agent-mail-cli doctor|mcp-agent-mail-cli guard)'

          if rg -n "${stale_pattern}" "${live_docs[@]}"; then
            echo "Found stale live-doc phrases above." >&2
            exit 1
          fi

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Build workspace
        run: cargo build --workspace

      - name: Unit + integration tests
        run: cargo test --workspace
        env:
          DATABASE_URL: "sqlite:///tmp/ci_test.sqlite3"
          STORAGE_ROOT: "/tmp/ci_storage"
          AGENT_NAME: "CiTestAgent"
          HTTP_HOST: "127.0.0.1"
          HTTP_PORT: "1"
          HTTP_PATH: "/mcp/"

      - name: Validate artifact bundles
        if: always()
        run: |
          set -euo pipefail
          source scripts/e2e_lib.sh
          e2e_validate_bundle_tree tests/artifacts

      - name: Upload test artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-artifacts
          path: tests/artifacts/
          retention-days: 14
          if-no-files-found: ignore

  # ── Mode matrix harness (Rust integration) ──────────────────────────
  mode-matrix:
    name: Mode Matrix
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Clone sibling dependencies
        shell: bash
        run: |
          git clone --depth 1 https://github.com/Dicklesworthstone/frankensearch ../frankensearch
          git -C ../frankensearch fetch --depth 1 origin f4f3faf7d610c355c28e83939ceea3c0d8481cc5
          git -C ../frankensearch checkout --detach FETCH_HEAD
          # frankensearch's tools/optimize_params workspace member depends on
          # ../../../fast_cmaes — required for cargo metadata/fmt/clippy on the
          # full workspace (cargo build -p X can skip it, but workspace-wide
          # commands cannot).
          git clone --depth 1 https://github.com/Dicklesworthstone/fast_cmaes ../fast_cmaes
          # 2026-08-22 registry universe: asupersync, fastmcp, sqlmodel, fsqlite,
          # ftui, franken-agent-detection, tru, and rich_rust all resolve from
          # crates.io. beads_rust stays path-patched (registry 0.3.2 pins
          # asupersync =0.4.4; the 0.4.0 line on main pins =0.4.9).
          git clone --depth 1 https://github.com/Dicklesworthstone/beads_rust ../beads_rust

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Build both binaries
        run: |
          cargo build -p mcp-agent-mail-cli
          cargo build -p mcp-agent-mail

      - name: Mode matrix harness (Rust)
        run: cargo test -p mcp-agent-mail-cli --test mode_matrix_harness -- --nocapture
        env:
          DATABASE_URL: "sqlite:///tmp/ci_matrix.sqlite3"
          STORAGE_ROOT: "/tmp/ci_storage"
          AGENT_NAME: "CiTestAgent"
          HTTP_HOST: "127.0.0.1"
          HTTP_PORT: "1"
          HTTP_PATH: "/mcp/"

      - name: Validate artifact bundles
        if: always()
        run: |
          set -euo pipefail
          source scripts/e2e_lib.sh
          e2e_validate_bundle_tree tests/artifacts

      - name: Upload matrix artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mode-matrix-artifacts
          path: tests/artifacts/cli/mode_matrix/
          retention-days: 14
          if-no-files-found: ignore

  # ── Cross-platform native command matrix ───────────────────────────
  native-command-matrix:
    name: Native Command Matrix (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    needs: test
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Clone sibling dependencies
        shell: bash
        run: |
          git clone --depth 1 https://github.com/Dicklesworthstone/frankensearch ../frankensearch
          git -C ../frankensearch fetch --depth 1 origin f4f3faf7d610c355c28e83939ceea3c0d8481cc5
          git -C ../frankensearch checkout --detach FETCH_HEAD
          # frankensearch's tools/optimize_params workspace member depends on
          # ../../../fast_cmaes — required for cargo metadata/fmt/clippy on the
          # full workspace (cargo build -p X can skip it, but workspace-wide
          # commands cannot).
          git clone --depth 1 https://github.com/Dicklesworthstone/fast_cmaes ../fast_cmaes
          # 2026-08-22 registry universe: asupersync, fastmcp, sqlmodel, fsqlite,
          # ftui, franken-agent-detection, tru, and rich_rust all resolve from
          # crates.io. beads_rust stays path-patched (registry 0.3.2 pins
          # asupersync =0.4.4; the 0.4.0 line on main pins =0.4.9).
          git clone --depth 1 https://github.com/Dicklesworthstone/beads_rust ../beads_rust

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Build CLI binary
        run: cargo build -p mcp-agent-mail-cli

      - name: Run native command portability matrix
        shell: bash
        run: |
          set -euo pipefail

          AM_BIN="target/debug/am"
          ARTIFACT_ROOT="tests/artifacts/cli/native_command_matrix/${{ matrix.os }}"
          mkdir -p "${ARTIFACT_ROOT}/commands"
          export ARTIFACT_ROOT

          PYTHON_BIN="python3"
          if ! command -v "${PYTHON_BIN}" >/dev/null 2>&1; then
            PYTHON_BIN="python"
          fi

          cat > "${ARTIFACT_ROOT}/environment.json" <<EOF
          {
            "runner_os": "${{ runner.os }}",
            "runner_arch": "${{ runner.arch }}",
            "matrix_os": "${{ matrix.os }}",
            "timestamp_utc": "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          }
          EOF

          run_case() {
            local case_id="$1"
            shift
            local case_dir="${ARTIFACT_ROOT}/commands/${case_id}"
            mkdir -p "${case_dir}"

            printf '%q ' "$@" > "${case_dir}/command.txt"
            printf '\n' >> "${case_dir}/command.txt"

            local started_ns ended_ns elapsed_ms exit_code
            started_ns="$("${PYTHON_BIN}" -c 'import time; print(time.time_ns())')"
            if "$@" > "${case_dir}/stdout.txt" 2> "${case_dir}/stderr.txt"; then
              exit_code=0
            else
              exit_code=$?
            fi
            ended_ns="$("${PYTHON_BIN}" -c 'import time; print(time.time_ns())')"
            elapsed_ms="$("${PYTHON_BIN}" -c "print(round((${ended_ns}-${started_ns})/1_000_000, 3))")"

            printf '%s\n' "${exit_code}" > "${case_dir}/exit_code.txt"
            printf '%s\n' "${elapsed_ms}" > "${case_dir}/timing_ms.txt"

            if [ "${exit_code}" -ne 0 ]; then
              echo "Command case ${case_id} failed with exit ${exit_code}"
              return 1
            fi
          }

          run_case ci_help "${AM_BIN}" ci --help
          run_case flake_triage_help "${AM_BIN}" flake-triage --help
          run_case bench_help "${AM_BIN}" bench --help
          run_case check_inbox_help "${AM_BIN}" check-inbox --help
          run_case golden_verify_help "${AM_BIN}" golden verify --help
          run_case serve_http_help "${AM_BIN}" serve-http --help
          run_case e2e_run_help "${AM_BIN}" e2e run --help
          run_case share_wizard_help "${AM_BIN}" share wizard --help
          run_case share_verify_live_help "${AM_BIN}" share deploy verify-live --help
          run_case doctor_check_help "${AM_BIN}" doctor check --help
          run_case check_inbox_direct_json "${AM_BIN}" check-inbox --direct --json --project "${PWD}" --agent MatrixProbe --rate-limit 0
          run_case config_set_port_envfile "${AM_BIN}" config set-port 8765 --env-file "${ARTIFACT_ROOT}/tmp env file.env"
          run_case config_show_port "${AM_BIN}" config show-port

          "${PYTHON_BIN}" - <<'PY'
          import json
          import os
          from pathlib import Path

          root = Path(os.environ["ARTIFACT_ROOT"])
          command_root = root / "commands"
          cases = []
          failures = 0

          for case_dir in sorted(p for p in command_root.iterdir() if p.is_dir()):
              exit_code = int((case_dir / "exit_code.txt").read_text(encoding="utf-8").strip())
              elapsed_ms = float((case_dir / "timing_ms.txt").read_text(encoding="utf-8").strip())
              cases.append(
                  {
                      "id": case_dir.name,
                      "exit_code": exit_code,
                      "elapsed_ms": elapsed_ms,
                  }
              )
              if exit_code != 0:
                  failures += 1

          summary = {
              "total": len(cases),
              "pass": len(cases) - failures,
              "fail": failures,
              "cases": cases,
          }
          (root / "summary.json").write_text(json.dumps(summary, indent=2) + "\n", encoding="utf-8")

          if failures:
              raise SystemExit(1)
          PY

      - name: Upload native matrix artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: native-command-matrix-${{ matrix.os }}
          path: tests/artifacts/cli/native_command_matrix/${{ matrix.os }}/
          retention-days: 14
          if-no-files-found: error

  # ── Semantic conformance ────────────────────────────────────────────
  conformance:
    name: Semantic Conformance
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Clone sibling dependencies
        shell: bash
        run: |
          git clone --depth 1 https://github.com/Dicklesworthstone/frankensearch ../frankensearch
          git -C ../frankensearch fetch --depth 1 origin f4f3faf7d610c355c28e83939ceea3c0d8481cc5
          git -C ../frankensearch checkout --detach FETCH_HEAD
          # frankensearch's tools/optimize_params workspace member depends on
          # ../../../fast_cmaes — required for cargo metadata/fmt/clippy on the
          # full workspace (cargo build -p X can skip it, but workspace-wide
          # commands cannot).
          git clone --depth 1 https://github.com/Dicklesworthstone/fast_cmaes ../fast_cmaes
          # 2026-08-22 registry universe: asupersync, fastmcp, sqlmodel, fsqlite,
          # ftui, franken-agent-detection, tru, and rich_rust all resolve from
          # crates.io. beads_rust stays path-patched (registry 0.3.2 pins
          # asupersync =0.4.4; the 0.4.0 line on main pins =0.4.9).
          git clone --depth 1 https://github.com/Dicklesworthstone/beads_rust ../beads_rust

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Build CLI binary
        run: cargo build -p mcp-agent-mail-cli

      - name: Semantic conformance suite
        run: cargo test -p mcp-agent-mail-cli --test semantic_conformance -- --nocapture
        env:
          DATABASE_URL: "sqlite:///tmp/ci_conform.sqlite3"
          STORAGE_ROOT: "/tmp/ci_storage"
          AGENT_NAME: "CiTestAgent"
          HTTP_HOST: "127.0.0.1"
          HTTP_PORT: "1"
          HTTP_PATH: "/mcp/"

      - name: Validate artifact bundles
        if: always()
        run: |
          set -euo pipefail
          source scripts/e2e_lib.sh
          e2e_validate_bundle_tree tests/artifacts

      - name: Upload conformance artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: conformance-artifacts
          path: tests/artifacts/cli/semantic_conformance/
          retention-days: 14
          if-no-files-found: ignore

  # ── Perf + security regressions ─────────────────────────────────────
  perf-security:
    name: Perf & Security
    runs-on: ubuntu-latest
    timeout-minutes: 25
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Clone sibling dependencies
        shell: bash
        run: |
          git clone --depth 1 https://github.com/Dicklesworthstone/frankensearch ../frankensearch
          git -C ../frankensearch fetch --depth 1 origin f4f3faf7d610c355c28e83939ceea3c0d8481cc5
          git -C ../frankensearch checkout --detach FETCH_HEAD
          # frankensearch's tools/optimize_params workspace member depends on
          # ../../../fast_cmaes — required for cargo metadata/fmt/clippy on the
          # full workspace (cargo build -p X can skip it, but workspace-wide
          # commands cannot).
          git clone --depth 1 https://github.com/Dicklesworthstone/fast_cmaes ../fast_cmaes
          # 2026-08-22 registry universe: asupersync, fastmcp, sqlmodel, fsqlite,
          # ftui, franken-agent-detection, tru, and rich_rust all resolve from
          # crates.io. beads_rust stays path-patched (registry 0.3.2 pins
          # asupersync =0.4.4; the 0.4.0 line on main pins =0.4.9).
          git clone --depth 1 https://github.com/Dicklesworthstone/beads_rust ../beads_rust

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Build both binaries
        run: |
          cargo build -p mcp-agent-mail-cli
          cargo build -p mcp-agent-mail

      - name: Perf + security regression suite
        run: cargo test -p mcp-agent-mail-cli --test perf_security_regressions -- --nocapture
        env:
          DATABASE_URL: "sqlite:///tmp/ci_perf.sqlite3"
          STORAGE_ROOT: "/tmp/ci_storage"
          AGENT_NAME: "CiTestAgent"
          HTTP_HOST: "127.0.0.1"
          HTTP_PORT: "1"
          HTTP_PATH: "/mcp/"

      - name: Soak harness perf trend suite (quick)
        run: am e2e run --project . soak_harness
        env:
          SOAK_SEED: "42"
          SOAK_PROJECTS: "3"
          SOAK_AGENTS_PER_PROJECT: "3"
          SUSTAINED_LOAD_RPS: "60"
          SUSTAINED_LOAD_SECS: "5"
          SOAK_DURATION_SECS: "5"

      - name: Install TUI terminal emulator
        run: |
          python3 -m venv /tmp/am-tui-pyte
          /tmp/am-tui-pyte/bin/pip install --disable-pip-version-check pyte==0.8.2
          echo "/tmp/am-tui-pyte/bin" >> "$GITHUB_PATH"

      - name: TUI traversal + flash + soak regression gate
        run: am e2e run --project . tui_full_traversal
        env:
          E2E_FIXTURE_PROFILE: "small"
          E2E_CAPTURE_BASELINE_PROFILE: "0"
          SOAK_DURATION_SECONDS: "120"

      - name: Verify perf trend artifacts
        if: always()
        run: |
          set -euo pipefail
          TREND_FILE="$(command find tests/artifacts/cli/perf_security -type f -name 'perf_timeseries.jsonl' -print -quit || true)"
          if [ -z "${TREND_FILE}" ]; then
            echo "Missing perf trend artifact (perf_timeseries.jsonl)"
            exit 1
          fi
          echo "Perf trend artifact: ${TREND_FILE}"

      - name: Verify soak trend artifacts
        if: always()
        run: |
          set -euo pipefail
          SOAK_TREND_FILE="$(command find tests/artifacts/perf/soak_harness -type f -name 'perf_timeseries.jsonl' -print -quit || true)"
          if [ -z "${SOAK_TREND_FILE}" ]; then
            echo "Missing soak trend artifact (perf_timeseries.jsonl)"
            exit 1
          fi
          echo "Soak trend artifact: ${SOAK_TREND_FILE}"

      - name: Verify TUI traversal/flash/soak gate artifacts
        if: always()
        run: |
          set -euo pipefail
          python3 <<'PY'
          import json
          from pathlib import Path

          root = Path("tests/artifacts/tui_full_traversal")
          runs = sorted(p for p in root.glob("*/") if p.is_dir())
          if not runs:
              print("Missing tests/artifacts/tui_full_traversal/<timestamp>/")
              raise SystemExit(1)
          run_dir = runs[-1]

          required_files = [
              "summary.json",
              "traversal_gate_verdict.json",
              "flash_detection_report.json",
              "soak_regression_report.json",
              "lag_flash_gate_triage.md",
          ]
          missing = [name for name in required_files if not (run_dir / name).is_file()]
          if missing:
              print(f"Missing required TUI gate artifacts in {run_dir}: {missing}")
              raise SystemExit(1)

          failures = []
          for report_name in (
              "traversal_gate_verdict.json",
              "flash_detection_report.json",
              "soak_regression_report.json",
          ):
              report = json.loads((run_dir / report_name).read_text(encoding="utf-8"))
              if report.get("all_within_budget") is not True:
                  failures.append(f"{report_name}: all_within_budget != true")

          summary = json.loads((run_dir / "summary.json").read_text(encoding="utf-8"))
          if int(summary.get("fail", 1)) != 0:
              failures.append(f"summary.json: fail={summary.get('fail')} (expected 0)")

          if failures:
              print("TUI incident regression gate failed:")
              for item in failures:
                  print(f" - {item}")
              triage_path = run_dir / "lag_flash_gate_triage.md"
              if triage_path.is_file():
                  print("--- lag_flash_gate_triage.md ---")
                  print(triage_path.read_text(encoding="utf-8"))
                  print("--- end lag_flash_gate_triage.md ---")
              raise SystemExit(1)

          print(f"TUI incident regression gate PASS ({run_dir})")
          print(f"Triage digest: {run_dir / 'lag_flash_gate_triage.md'}")
          PY

      - name: Validate artifact bundles
        if: always()
        run: |
          set -euo pipefail
          source scripts/e2e_lib.sh
          e2e_validate_bundle_tree tests/artifacts

      - name: Upload perf/security artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: perf-security-artifacts
          path: |
            tests/artifacts/cli/perf_security/
            tests/artifacts/perf/soak_harness/
            tests/artifacts/soak/multi_project/
            tests/artifacts/tui/soak_replay/
            tests/artifacts/tui_full_traversal/
          retention-days: 14
          if-no-files-found: ignore

  # ── E2E dual-mode native suite ──────────────────────────────────────
  e2e-dual-mode:
    name: E2E Dual-Mode (Native)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Clone sibling dependencies
        shell: bash
        run: |
          git clone --depth 1 https://github.com/Dicklesworthstone/frankensearch ../frankensearch
          git -C ../frankensearch fetch --depth 1 origin f4f3faf7d610c355c28e83939ceea3c0d8481cc5
          git -C ../frankensearch checkout --detach FETCH_HEAD
          # frankensearch's tools/optimize_params workspace member depends on
          # ../../../fast_cmaes — required for cargo metadata/fmt/clippy on the
          # full workspace (cargo build -p X can skip it, but workspace-wide
          # commands cannot).
          git clone --depth 1 https://github.com/Dicklesworthstone/fast_cmaes ../fast_cmaes
          # 2026-08-22 registry universe: asupersync, fastmcp, sqlmodel, fsqlite,
          # ftui, franken-agent-detection, tru, and rich_rust all resolve from
          # crates.io. beads_rust stays path-patched (registry 0.3.2 pins
          # asupersync =0.4.4; the 0.4.0 line on main pins =0.4.9).
          git clone --depth 1 https://github.com/Dicklesworthstone/beads_rust ../beads_rust

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Build both binaries
        run: |
          cargo build -p mcp-agent-mail-cli
          cargo build -p mcp-agent-mail

      - name: E2E health + doctor selftests suite
        # Runs doctor selftest, mcp-selftest, and write-selftest end-to-end and
        # requires ok=true. This is the gate that would have caught GH#203 at
        # v0.3.22: a total read-path hang shipped twice because nothing in CI
        # ever ran the binary end-to-end over the MCP transport.
        run: am e2e run --project . health_and_selftests

      - name: E2E dual-mode suite
        run: am e2e run --project . dual_mode

      - name: E2E mode matrix suite
        run: am e2e run --project . mode_matrix

      - name: E2E web parity suite (HTTP wrapper, includes mail_ui)
        run: am e2e run --project . http

      - name: E2E static export conformance suite
        run: am e2e run --project . share

      - name: E2E cross-epic integration suite
        run: am e2e run --project . cross_epic_integration

      - name: Validate artifact bundles
        if: always()
        run: |
          set -euo pipefail
          source scripts/e2e_lib.sh
          e2e_validate_bundle_tree tests/artifacts

      - name: Upload E2E artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-artifacts
          path: tests/artifacts/
          retention-days: 14
          if-no-files-found: ignore

  # ── Search V3 E2E + diagnostics contract ──────────────────────────────
  search-v3-e2e:
    name: Search V3 E2E + Diagnostics
    runs-on: ubuntu-latest
    timeout-minutes: 40
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Clone sibling dependencies
        shell: bash
        run: |
          git clone --depth 1 https://github.com/Dicklesworthstone/frankensearch ../frankensearch
          git -C ../frankensearch fetch --depth 1 origin f4f3faf7d610c355c28e83939ceea3c0d8481cc5
          git -C ../frankensearch checkout --detach FETCH_HEAD
          # frankensearch's tools/optimize_params workspace member depends on
          # ../../../fast_cmaes — required for cargo metadata/fmt/clippy on the
          # full workspace (cargo build -p X can skip it, but workspace-wide
          # commands cannot).
          git clone --depth 1 https://github.com/Dicklesworthstone/fast_cmaes ../fast_cmaes
          # 2026-08-22 registry universe: asupersync, fastmcp, sqlmodel, fsqlite,
          # ftui, franken-agent-detection, tru, and rich_rust all resolve from
          # crates.io. beads_rust stays path-patched (registry 0.3.2 pins
          # asupersync =0.4.4; the 0.4.0 line on main pins =0.4.9).
          git clone --depth 1 https://github.com/Dicklesworthstone/beads_rust ../beads_rust

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Build both binaries
        run: |
          cargo build -p mcp-agent-mail-cli
          cargo build -p mcp-agent-mail

      - name: Run mandatory Search V3 E2E suites
        run: |
          set -euo pipefail
          mkdir -p tests/artifacts/search_v3_ci

          # Native runner dispatches all 5 suites and reports results.
          # Environment variables pass through to underlying test scripts.
          if am e2e run --project . \
            search_v3_stdio \
            search_v3_http \
            search_v3_shadow_parity \
            search_v3_resilience \
            search_v3_load_concurrency \
            2>&1 | tee tests/artifacts/search_v3_ci/native_runner.log; then
            FAILED=0
          else
            FAILED=1
          fi

          cat > tests/artifacts/search_v3_ci/required_suites.txt <<'EOF'
          search_v3_stdio
          search_v3_http
          search_v3_shadow_parity
          search_v3_resilience
          search_v3_load_concurrency
          EOF

          echo "SEARCH_V3_E2E_FAILED=${FAILED}" >> "${GITHUB_ENV}"
        env:
          E2E_CLOCK_MODE: "deterministic"
          E2E_SEED: "424242"
          SEARCH_V3_LOG_ROOT: "${{ github.workspace }}/tests/artifacts/search_v3"
          SV3_ARTIFACT_ROOT: "${{ github.workspace }}/tests/artifacts/search_v3"

      - name: Run Search V3 diagnostics rust tests
        if: always()
        run: |
          set -euo pipefail
          mkdir -p tests/artifacts/search_v3_ci
          FAILED=0

          run_diag_test() {
            local test_name="$1"
            local log_file="tests/artifacts/search_v3_ci/${test_name}.log"
            echo "::group::${test_name}"
            if cargo test -p mcp-agent-mail-db --test "${test_name}" -- --nocapture 2>&1 | tee "${log_file}"; then
              echo "PASS: ${test_name}"
            else
              echo "FAIL: ${test_name}"
              FAILED=1
            fi
            echo "::endgroup::"
          }

          run_diag_test filter_pagination
          run_diag_test logging_redaction
          run_diag_test diversity_dedup
          run_diag_test timeout_backpressure

          echo "SEARCH_V3_DIAG_FAILED=${FAILED}" >> "${GITHUB_ENV}"

      - name: Verify Search V3 artifact + assertion contract
        if: always()
        run: |
          set -euo pipefail
          python3 <<'PY'
          import json
          import sys
          from pathlib import Path

          artifact_root = Path("tests/artifacts")
          errors: list[str] = []

          floors = {
              "search_v3_stdio": 80,
              "search_v3_http": 90,
              "search_v3_shadow_parity": 1,
              "search_v3_resilience": 70,
              "search_v3_load_concurrency": 60,
          }

          def newest(paths):
              return sorted(paths, key=lambda p: str(p))[-1] if paths else None

          for suite, floor in floors.items():
              summary = newest((artifact_root / suite).glob("*/summary.json"))
              if summary is None:
                  errors.append(f"{suite}: missing tests/artifacts/{suite}/*/summary.json")
                  continue
              data = json.loads(summary.read_text(encoding="utf-8"))
              total = int(data.get("total", -1))
              failed = int(data.get("fail", -1))
              if total < floor:
                  errors.append(f"{suite}: assertion floor violated ({total} < {floor}) in {summary}")
              if failed != 0:
                  errors.append(f"{suite}: non-zero failures ({failed}) in {summary}")

          structured_expectations = {
              "search_v3_stdio": [
                  "summaries/suite_summary.json",
                  "summaries/suite_summary.txt",
                  "logs/summary.log",
              ],
              "search_v3_http": [
                  "summaries/suite_summary.json",
                  "summaries/suite_summary.txt",
                  "logs/summary.log",
              ],
              "search_v3_shadow_parity": [
                  "summaries/suite_summary.json",
                  "summaries/suite_summary.txt",
                  "logs/summary.log",
              ],
              "search_v3_load_concurrency": [
                  "summaries/suite_summary.json",
                  "summaries/suite_summary.txt",
                  "logs/summary.log",
              ],
              "search_v3_resilience": [
                  "summary.json",
                  "summary.txt",
                  "run_metadata.json",
              ],
          }

          sv3_root = artifact_root / "search_v3"
          for suite, rel_files in structured_expectations.items():
              run_root = newest((sv3_root / suite).glob("*/"))
              if run_root is None:
                  errors.append(f"{suite}: missing structured root under {sv3_root / suite}")
                  continue
              for rel in rel_files:
                  target = run_root / rel
                  if not target.is_file():
                      errors.append(f"{suite}: missing structured artifact {target}")

          for test_name in ("filter_pagination", "logging_redaction", "diversity_dedup", "timeout_backpressure"):
              log_path = artifact_root / "search_v3_ci" / f"{test_name}.log"
              if not log_path.is_file():
                  errors.append(f"missing diagnostics test log: {log_path}")

          if errors:
              print("Search V3 CI contract verification failed:")
              for err in errors:
                  print(f" - {err}")
              sys.exit(1)

          print("Search V3 CI contract verification passed.")
          PY

      - name: Enforce Search V3 pass/fail contract
        if: always()
        run: |
          set -euo pipefail
          E2E_FAILED="${SEARCH_V3_E2E_FAILED:-1}"
          DIAG_FAILED="${SEARCH_V3_DIAG_FAILED:-1}"
          echo "SEARCH_V3_E2E_FAILED=${E2E_FAILED}"
          echo "SEARCH_V3_DIAG_FAILED=${DIAG_FAILED}"
          if [ "${E2E_FAILED}" -ne 0 ] || [ "${DIAG_FAILED}" -ne 0 ]; then
            echo "Search V3 suites/tests reported failures."
            exit 1
          fi

      - name: Validate artifact bundles
        if: always()
        run: |
          set -euo pipefail
          source scripts/e2e_lib.sh
          e2e_validate_bundle_tree tests/artifacts

      - name: Upload Search V3 artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: search-v3-artifacts
          path: |
            tests/artifacts/search_v3_*/
            tests/artifacts/search_v3/
            tests/artifacts/search_v3_ci/
          retention-days: 14
          if-no-files-found: error

  # ── Incident regression gates (br-2k3qx non-regression) ────────────
  incident-regression-gates:
    name: Incident Regression Gates
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Clone sibling dependencies
        shell: bash
        run: |
          git clone --depth 1 https://github.com/Dicklesworthstone/frankensearch ../frankensearch
          git -C ../frankensearch fetch --depth 1 origin f4f3faf7d610c355c28e83939ceea3c0d8481cc5
          git -C ../frankensearch checkout --detach FETCH_HEAD
          # frankensearch's tools/optimize_params workspace member depends on
          # ../../../fast_cmaes — required for cargo metadata/fmt/clippy on the
          # full workspace (cargo build -p X can skip it, but workspace-wide
          # commands cannot).
          git clone --depth 1 https://github.com/Dicklesworthstone/fast_cmaes ../fast_cmaes
          # 2026-08-22 registry universe: asupersync, fastmcp, sqlmodel, fsqlite,
          # ftui, franken-agent-detection, tru, and rich_rust all resolve from
          # crates.io. beads_rust stays path-patched (registry 0.3.2 pins
          # asupersync =0.4.4; the 0.4.0 line on main pins =0.4.9).
          git clone --depth 1 https://github.com/Dicklesworthstone/beads_rust ../beads_rust

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Build both binaries
        run: |
          cargo build -p mcp-agent-mail-cli
          cargo build -p mcp-agent-mail

      - name: Run CI Oracle Gate
        id: oracle
        run: |
          set -uo pipefail
          GATE_DIR="tests/artifacts/ci_oracle_gate/$(date -u '+%Y%m%d_%H%M%S')"
          mkdir -p "${GATE_DIR}"
          echo "GATE_DIR=${GATE_DIR}" >> "${GITHUB_ENV}"

          GATE_RC=0
          scripts/ci_oracle_gate.sh \
            --mode deterministic \
            --output-dir "${GATE_DIR}" \
            --robot-timeout-secs 10 \
            --verbose || GATE_RC=$?

          echo "GATE_RC=${GATE_RC}" >> "${GITHUB_ENV}"

          # Exit 2 (WARN) is acceptable — only exit 1 is a gate failure
          if [ "${GATE_RC}" -eq 1 ]; then
            echo "Oracle gate FAIL — proceeding to regression classification"
          elif [ "${GATE_RC}" -eq 2 ]; then
            echo "Oracle gate WARN — whitelisted mismatches only"
          elif [ "${GATE_RC}" -eq 0 ]; then
            echo "Oracle gate PASS"
          else
            echo "Oracle gate unexpected exit: ${GATE_RC}"
          fi
        env:
          DATABASE_URL: "sqlite:///tmp/ci_incident.sqlite3"
          STORAGE_ROOT: "/tmp/ci_storage"
          AGENT_NAME: "CiTestAgent"
          HTTP_HOST: "127.0.0.1"
          HTTP_PORT: "1"
          HTTP_PATH: "/mcp/"

      - name: Verify incident regression categories
        if: always()
        run: |
          set -euo pipefail
          GATE_DIR="${GATE_DIR:-tests/artifacts/ci_oracle_gate/latest}"
          python3 scripts/verify_incident_gates.py "${GATE_DIR}"

      - name: Upload incident gate artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: incident-regression-gate-artifacts
          path: tests/artifacts/ci_oracle_gate/
          retention-days: 14
          if-no-files-found: ignore

  # ── Help snapshots (golden contract drift detection) ────────────────
  snapshots:
    name: Help Snapshots
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Clone sibling dependencies
        shell: bash
        run: |
          git clone --depth 1 https://github.com/Dicklesworthstone/frankensearch ../frankensearch
          git -C ../frankensearch fetch --depth 1 origin f4f3faf7d610c355c28e83939ceea3c0d8481cc5
          git -C ../frankensearch checkout --detach FETCH_HEAD
          # frankensearch's tools/optimize_params workspace member depends on
          # ../../../fast_cmaes — required for cargo metadata/fmt/clippy on the
          # full workspace (cargo build -p X can skip it, but workspace-wide
          # commands cannot).
          git clone --depth 1 https://github.com/Dicklesworthstone/fast_cmaes ../fast_cmaes
          # 2026-08-22 registry universe: asupersync, fastmcp, sqlmodel, fsqlite,
          # ftui, franken-agent-detection, tru, and rich_rust all resolve from
          # crates.io. beads_rust stays path-patched (registry 0.3.2 pins
          # asupersync =0.4.4; the 0.4.0 line on main pins =0.4.9).
          git clone --depth 1 https://github.com/Dicklesworthstone/beads_rust ../beads_rust

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Build CLI binary
        run: cargo build -p mcp-agent-mail-cli

      - name: Verify help snapshots
        run: cargo test -p mcp-agent-mail-cli --test help_snapshots -- --nocapture
        env:
          COLUMNS: "120"
          DATABASE_URL: "sqlite:///tmp/ci_snap.sqlite3"
          STORAGE_ROOT: "/tmp/ci_storage"
          AGENT_NAME: "CiTestAgent"
          HTTP_HOST: "127.0.0.1"
          HTTP_PORT: "1"
          HTTP_PATH: "/mcp/"

      - name: Validate artifact bundles
        if: always()
        run: |
          set -euo pipefail
          source scripts/e2e_lib.sh
          e2e_validate_bundle_tree tests/artifacts

      - name: Upload snapshot diff artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: snapshot-diff-artifacts
          path: tests/artifacts/cli/help/
          retention-days: 14
          if-no-files-found: ignore

  # ── Durability unit tests (br-97gc6.5.2.6.5.1) ─────────────────────
  # Exercises the mailbox durability state machine, verdict engine,
  # forensic bundle capture, and archive reconstruction logic.
  durability-unit:
    name: Durability Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Clone sibling dependencies
        shell: bash
        run: |
          git clone --depth 1 https://github.com/Dicklesworthstone/frankensearch ../frankensearch
          git -C ../frankensearch fetch --depth 1 origin f4f3faf7d610c355c28e83939ceea3c0d8481cc5
          git -C ../frankensearch checkout --detach FETCH_HEAD
          # frankensearch's tools/optimize_params workspace member depends on
          # ../../../fast_cmaes — required for cargo metadata/fmt/clippy on the
          # full workspace (cargo build -p X can skip it, but workspace-wide
          # commands cannot).
          git clone --depth 1 https://github.com/Dicklesworthstone/fast_cmaes ../fast_cmaes
          # 2026-08-22 registry universe: asupersync, fastmcp, sqlmodel, fsqlite,
          # ftui, franken-agent-detection, tru, and rich_rust all resolve from
          # crates.io. beads_rust stays path-patched (registry 0.3.2 pins
          # asupersync =0.4.4; the 0.4.0 line on main pins =0.4.9).
          git clone --depth 1 https://github.com/Dicklesworthstone/beads_rust ../beads_rust

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Durability state machine tests (core)
        run: cargo test -p mcp-agent-mail-core --lib mailbox_durability -- --nocapture
        env:
          DATABASE_URL: "sqlite:///tmp/ci_durability.sqlite3"
          STORAGE_ROOT: "/tmp/ci_storage"
          AGENT_NAME: "CiTestAgent"
          HTTP_HOST: "127.0.0.1"
          HTTP_PORT: "1"
          HTTP_PATH: "/mcp/"

      - name: Verdict engine tests (db)
        run: cargo test -p mcp-agent-mail-db --lib mailbox_verdict -- --nocapture
        env:
          DATABASE_URL: "sqlite:///tmp/ci_durability.sqlite3"
          STORAGE_ROOT: "/tmp/ci_storage"
          AGENT_NAME: "CiTestAgent"
          HTTP_HOST: "127.0.0.1"
          HTTP_PORT: "1"
          HTTP_PATH: "/mcp/"

      - name: Forensic bundle tests (db)
        run: cargo test -p mcp-agent-mail-db --lib forensics -- --nocapture
        env:
          DATABASE_URL: "sqlite:///tmp/ci_durability.sqlite3"
          STORAGE_ROOT: "/tmp/ci_storage"
          AGENT_NAME: "CiTestAgent"
          HTTP_HOST: "127.0.0.1"
          HTTP_PORT: "1"
          HTTP_PATH: "/mcp/"

      - name: Reconstruction tests (db)
        run: cargo test -p mcp-agent-mail-db --lib reconstruct -- --nocapture
        env:
          DATABASE_URL: "sqlite:///tmp/ci_durability.sqlite3"
          STORAGE_ROOT: "/tmp/ci_storage"
          AGENT_NAME: "CiTestAgent"
          HTTP_HOST: "127.0.0.1"
          HTTP_PORT: "1"
          HTTP_PATH: "/mcp/"

      - name: Durability probe tests (db queries)
        run: cargo test -p mcp-agent-mail-db --lib durability_probe -- --nocapture
        env:
          DATABASE_URL: "sqlite:///tmp/ci_durability.sqlite3"
          STORAGE_ROOT: "/tmp/ci_storage"
          AGENT_NAME: "CiTestAgent"
          HTTP_HOST: "127.0.0.1"
          HTTP_PORT: "1"
          HTTP_PATH: "/mcp/"

      - name: Verify durability test count floor
        if: always()
        run: |
          set -euo pipefail
          TOTAL=0
          for module in mailbox_durability mailbox_verdict forensics reconstruct durability_probe; do
            COUNT="$(cargo test -p mcp-agent-mail-db --lib "${module}" -- --list 2>/dev/null | grep -c '::.*: test$' || true)"
            if [ "${COUNT}" -eq 0 ]; then
              COUNT="$(cargo test -p mcp-agent-mail-core --lib "${module}" -- --list 2>/dev/null | grep -c '::.*: test$' || true)"
            fi
            echo "${module}: ${COUNT} tests"
            TOTAL=$((TOTAL + COUNT))
          done
          echo "Total durability unit tests: ${TOTAL}"
          if [ "${TOTAL}" -lt 50 ]; then
            echo "FAIL: expected at least 50 durability unit tests, got ${TOTAL}"
            exit 1
          fi

      - name: Upload durability unit artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: durability-unit-artifacts
          path: |
            tests/artifacts/durability/
            /tmp/ci_durability.sqlite3
          retention-days: 14
          if-no-files-found: ignore

  # ── Durability E2E tests (br-97gc6.5.2.6.5.1) ─────────────────────
  # Runs end-to-end durability scenarios: crash-restart recovery,
  # database corruption handling, and failure/fault injection.
  durability-e2e:
    name: Durability E2E
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Clone sibling dependencies
        shell: bash
        run: |
          git clone --depth 1 https://github.com/Dicklesworthstone/frankensearch ../frankensearch
          git -C ../frankensearch fetch --depth 1 origin f4f3faf7d610c355c28e83939ceea3c0d8481cc5
          git -C ../frankensearch checkout --detach FETCH_HEAD
          # frankensearch's tools/optimize_params workspace member depends on
          # ../../../fast_cmaes — required for cargo metadata/fmt/clippy on the
          # full workspace (cargo build -p X can skip it, but workspace-wide
          # commands cannot).
          git clone --depth 1 https://github.com/Dicklesworthstone/fast_cmaes ../fast_cmaes
          # 2026-08-22 registry universe: asupersync, fastmcp, sqlmodel, fsqlite,
          # ftui, franken-agent-detection, tru, and rich_rust all resolve from
          # crates.io. beads_rust stays path-patched (registry 0.3.2 pins
          # asupersync =0.4.4; the 0.4.0 line on main pins =0.4.9).
          git clone --depth 1 https://github.com/Dicklesworthstone/beads_rust ../beads_rust

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Build both binaries
        run: |
          cargo build -p mcp-agent-mail-cli
          cargo build -p mcp-agent-mail

      - name: E2E crash-restart recovery
        id: crash_restart
        if: always()
        run: am e2e run --project . crash_restart
        env:
          DATABASE_URL: "sqlite:///tmp/ci_dur_e2e.sqlite3"
          STORAGE_ROOT: "/tmp/ci_dur_storage"
          AGENT_NAME: "CiTestAgent"
          HTTP_HOST: "127.0.0.1"
          HTTP_PORT: "1"
          HTTP_PATH: "/mcp/"

      - name: E2E database corruption handling
        id: db_corruption
        if: always()
        run: am e2e run --project . db_corruption
        env:
          DATABASE_URL: "sqlite:///tmp/ci_dur_e2e.sqlite3"
          STORAGE_ROOT: "/tmp/ci_dur_storage"
          AGENT_NAME: "CiTestAgent"
          HTTP_HOST: "127.0.0.1"
          HTTP_PORT: "1"
          HTTP_PATH: "/mcp/"

      - name: E2E failure injection
        id: failure_injection
        if: always()
        run: am e2e run --project . failure_injection
        env:
          DATABASE_URL: "sqlite:///tmp/ci_dur_e2e.sqlite3"
          STORAGE_ROOT: "/tmp/ci_dur_storage"
          AGENT_NAME: "CiTestAgent"
          HTTP_HOST: "127.0.0.1"
          HTTP_PORT: "1"
          HTTP_PATH: "/mcp/"

      - name: E2E fault injection
        id: fault_injection
        if: always()
        run: am e2e run --project . fault_injection
        env:
          DATABASE_URL: "sqlite:///tmp/ci_dur_e2e.sqlite3"
          STORAGE_ROOT: "/tmp/ci_dur_storage"
          AGENT_NAME: "CiTestAgent"
          HTTP_HOST: "127.0.0.1"
          HTTP_PORT: "1"
          HTTP_PATH: "/mcp/"

      - name: Summarize durability E2E results
        if: always()
        run: |
          set -euo pipefail
          mkdir -p tests/artifacts/durability_e2e_ci
          FAILED=0
          for suite in crash_restart db_corruption failure_injection fault_injection; do
            SUMMARY="$(command find tests/artifacts -path "*/${suite}/*/summary.json" -print -quit 2>/dev/null || true)"
            if [ -z "${SUMMARY}" ]; then
              echo "WARN: no summary.json found for ${suite}"
              FAILED=1
              continue
            fi
            FAIL_COUNT="$(python3 -c "import json,pathlib; d=json.loads(pathlib.Path('${SUMMARY}').read_text()); print(d.get('fail',0))")"
            TOTAL="$(python3 -c "import json,pathlib; d=json.loads(pathlib.Path('${SUMMARY}').read_text()); print(d.get('total',0))")"
            echo "${suite}: ${TOTAL} total, ${FAIL_COUNT} failed"
            if [ "${FAIL_COUNT}" -ne 0 ]; then
              FAILED=1
            fi
          done
          if [ "${FAILED}" -ne 0 ]; then
            echo "Durability E2E failures detected."
            exit 1
          fi
          echo "All durability E2E suites passed."

      - name: Validate artifact bundles
        if: always()
        run: |
          set -euo pipefail
          source scripts/e2e_lib.sh
          e2e_validate_bundle_tree tests/artifacts

      - name: Upload durability E2E artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: durability-e2e-artifacts
          path: |
            tests/artifacts/crash_restart/
            tests/artifacts/db_corruption/
            tests/artifacts/failure_injection/
            tests/artifacts/fault_injection/
            tests/artifacts/durability_e2e_ci/
          retention-days: 14
          if-no-files-found: ignore

  # ── Chaos tests (br-97gc6.5.2.6.5.1) ──────────────────────────────
  # Circuit breaker fault injection, concurrent failure recovery, and
  # data integrity under chaos conditions.
  chaos:
    name: Chaos Tests
    runs-on: ubuntu-latest
    timeout-minutes: 25
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Clone sibling dependencies
        shell: bash
        run: |
          git clone --depth 1 https://github.com/Dicklesworthstone/frankensearch ../frankensearch
          git -C ../frankensearch fetch --depth 1 origin f4f3faf7d610c355c28e83939ceea3c0d8481cc5
          git -C ../frankensearch checkout --detach FETCH_HEAD
          # frankensearch's tools/optimize_params workspace member depends on
          # ../../../fast_cmaes — required for cargo metadata/fmt/clippy on the
          # full workspace (cargo build -p X can skip it, but workspace-wide
          # commands cannot).
          git clone --depth 1 https://github.com/Dicklesworthstone/fast_cmaes ../fast_cmaes
          # 2026-08-22 registry universe: asupersync, fastmcp, sqlmodel, fsqlite,
          # ftui, franken-agent-detection, tru, and rich_rust all resolve from
          # crates.io. beads_rust stays path-patched (registry 0.3.2 pins
          # asupersync =0.4.4; the 0.4.0 line on main pins =0.4.9).
          git clone --depth 1 https://github.com/Dicklesworthstone/beads_rust ../beads_rust

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Build workspace
        run: cargo build --workspace

      - name: Chaos circuit-breaker tests
        run: cargo test -p mcp-agent-mail-db --test stress chaos_ -- --nocapture
        env:
          DATABASE_URL: "sqlite:///tmp/ci_chaos.sqlite3"
          STORAGE_ROOT: "/tmp/ci_chaos_storage"
          AGENT_NAME: "CiTestAgent"
          HTTP_HOST: "127.0.0.1"
          HTTP_PORT: "1"
          HTTP_PATH: "/mcp/"

      - name: Fault injection integration tests
        run: cargo test -p mcp-agent-mail-db --test fault_injection -- --nocapture
        env:
          DATABASE_URL: "sqlite:///tmp/ci_chaos.sqlite3"
          STORAGE_ROOT: "/tmp/ci_chaos_storage"
          AGENT_NAME: "CiTestAgent"
          HTTP_HOST: "127.0.0.1"
          HTTP_PORT: "1"
          HTTP_PATH: "/mcp/"

      - name: Stress + pool exhaustion under failure
        run: |
          set -euo pipefail
          cargo test -p mcp-agent-mail-db --test stress -- --nocapture 2>&1 | tee /tmp/chaos_stress.log
          cargo test -p mcp-agent-mail-db --test pool_exhaustion -- --nocapture 2>&1 | tee /tmp/chaos_pool.log
        env:
          DATABASE_URL: "sqlite:///tmp/ci_chaos.sqlite3"
          STORAGE_ROOT: "/tmp/ci_chaos_storage"
          AGENT_NAME: "CiTestAgent"
          HTTP_HOST: "127.0.0.1"
          HTTP_PORT: "1"
          HTTP_PATH: "/mcp/"

      - name: Verify chaos test count floor
        if: always()
        run: |
          set -euo pipefail
          CHAOS_COUNT="$(cargo test -p mcp-agent-mail-db --test stress chaos_ -- --list 2>/dev/null | grep -c '::.*: test$' || echo 0)"
          echo "Chaos circuit-breaker tests: ${CHAOS_COUNT}"
          if [ "${CHAOS_COUNT}" -lt 5 ]; then
            echo "FAIL: expected at least 5 chaos tests, got ${CHAOS_COUNT}"
            exit 1
          fi

      - name: Collect chaos failure digest
        if: failure()
        run: |
          set -euo pipefail
          DIGEST_DIR="tests/artifacts/chaos_ci"
          mkdir -p "${DIGEST_DIR}"
          for logfile in /tmp/chaos_stress.log /tmp/chaos_pool.log; do
            if [ -f "${logfile}" ]; then
              BASENAME="$(basename "${logfile}")"
              cp "${logfile}" "${DIGEST_DIR}/${BASENAME}"
              # Extract failure lines for terse summary
              grep -E 'FAIL|panicked|error\[' "${logfile}" > "${DIGEST_DIR}/${BASENAME%.log}_failures.txt" 2>/dev/null || true
            fi
          done
          # Write a terse failure digest
          python3 <<'PY'
          import json
          from pathlib import Path
          digest_dir = Path("tests/artifacts/chaos_ci")
          failures = []
          for f in sorted(digest_dir.glob("*_failures.txt")):
              lines = f.read_text(encoding="utf-8").strip().splitlines()
              if lines:
                  failures.append({"file": f.name, "count": len(lines), "first_5": lines[:5]})
          digest = {"chaos_failure_count": sum(f["count"] for f in failures), "files": failures}
          (digest_dir / "failure_digest.json").write_text(json.dumps(digest, indent=2) + "\n", encoding="utf-8")
          print(json.dumps(digest, indent=2))
          PY

      - name: Upload chaos test artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: chaos-test-artifacts
          path: |
            tests/artifacts/chaos_ci/
            /tmp/chaos_stress.log
            /tmp/chaos_pool.log
          retention-days: 14
          if-no-files-found: ignore

  # ── Skip guard ──────────────────────────────────────────────────────
  # Prevents silent test-skip regressions by asserting non-zero test counts.
  skip-guard:
    name: Skip Guard
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [test, mode-matrix, native-command-matrix, conformance, perf-security, e2e-dual-mode, search-v3-e2e, incident-regression-gates, snapshots, durability-unit, durability-e2e, chaos]
    if: always()
    steps:
      - name: Verify all required jobs passed
        run: |
          echo "Job results:"
          echo "  test: ${{ needs.test.result }}"
          echo "  mode-matrix: ${{ needs.mode-matrix.result }}"
          echo "  native-command-matrix: ${{ needs.native-command-matrix.result }}"
          echo "  conformance: ${{ needs.conformance.result }}"
          echo "  perf-security: ${{ needs.perf-security.result }}"
          echo "  e2e-dual-mode: ${{ needs.e2e-dual-mode.result }}"
          echo "  search-v3-e2e: ${{ needs.search-v3-e2e.result }}"
          echo "  incident-regression-gates: ${{ needs.incident-regression-gates.result }}"
          echo "  snapshots: ${{ needs.snapshots.result }}"
          echo "  durability-unit: ${{ needs.durability-unit.result }}"
          echo "  durability-e2e: ${{ needs.durability-e2e.result }}"
          echo "  chaos: ${{ needs.chaos.result }}"

          FAILED=0
          for result in \
            "${{ needs.test.result }}" \
            "${{ needs.mode-matrix.result }}" \
            "${{ needs.native-command-matrix.result }}" \
            "${{ needs.conformance.result }}" \
            "${{ needs.perf-security.result }}" \
            "${{ needs.e2e-dual-mode.result }}" \
            "${{ needs.search-v3-e2e.result }}" \
            "${{ needs.incident-regression-gates.result }}" \
            "${{ needs.snapshots.result }}" \
            "${{ needs.durability-unit.result }}" \
            "${{ needs.durability-e2e.result }}" \
            "${{ needs.chaos.result }}"; do
            if [ "$result" != "success" ]; then
              echo "FAIL: job result = $result"
              FAILED=1
            fi
          done

          if [ "$FAILED" -eq 1 ]; then
            echo "One or more required jobs did not succeed."
            exit 1
          fi
          echo "All required jobs passed."
